<div class="flex items-center gap-3  py-2 ">

      <div class="bg-gradient-to-r p-[2px] ring-2 ring-white from-purple-400 via-pink-500 to-red-500 rounded-full shrink-0 inline-flex items-center justify-center overflow-hidden border border-gray-200 dark:border-secondary-500 text-base h-9 w-9" wire:ignore>
    <x-avatar
        wire:ignore
        src="{{ auth()->user()->avatar ? asset('storage/' . auth()->user()->avatar) : asset('assets/RDI.png') }}"
        class="h-full w-full object-cover"
    />
</div>
    <div class="grid grid-cols-7 w-full gap-2">
        {{-- comment --}}
        <div class="col-span-6 flex flex-wrap text-sm">

            <p>
                <span class="font-bold text-sm"> {{$comment->user->name}} </span>
                {{$comment->body}}
            </p>
        </div>

        {{-- Like --}}
        <div class="col-span-1 flex text-right justify-end mb-auto">
            <button class="font-bold text-sm  0 ml-auto">
                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5"
                    stroke="currentColor" class="w-3 h-3">
                    <path stroke-linecap="round" stroke-linejoin="round"
                        d="M21 8.25c0-2.485-2.099-4.5-4.688-4.5-1.935 0-3.597 1.126-4.312 2.733-.715-1.607-2.377-2.733-4.313-2.733C5.1 3.75 3 5.765 3 8.25c0 7.22 9 12 9 12s9-4.78 9-12z" />
                </svg>

            </button>
        </div>

        {{-- Footer --}}
        <div class="col-span-7 flex gap-2 text-xs text-gray-700">

            <span class=""> {{$comment->created_at->diffForHumans()}} </span>
            <span class="font-bold"> 345 Likes</span>
            <button wire:click="setParent({{$comment->id}})" class="font-semibold"> Reply</button>

        </div>
    </div>

</div>